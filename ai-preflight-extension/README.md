# 印前检查-雪糕

Adobe Illustrator 的**印前检查面板**（CEP 扩展）。点一下就把文档里常见的印前问题
扫一遍，并把能在面板里直接做的动作（文字转曲、图片嵌入）一并做了。

当前版本：**v8.3** · 完整变更日志见 [README.txt](README.txt)

---

## 功能

点「开始检查」后，面板依次给出 **6 张检查卡**。六张卡**始终全部渲染**——没有问题也
会显示「无」，方便确认检查确实跑过。

| 卡片 | 检查内容 |
| --- | --- |
| ① 画板 · 出血 · 色彩 | 画板尺寸；逐画板实测出血；色彩模式徽标 |
| ② 隐藏对象 | 被隐藏的对象与隐藏图层 |
| ③ 字体转曲 | 未转曲文本框、字体清单、缺失字体；极小字号 |
| ④ 图片嵌入 | 嵌入图片与链接图片数量、未嵌入清单 |
| ⑤ 图片分辨率 | 低于 300ppi 的图片清单 |
| ⑥ 油墨 · 叠印 · 描边粗细 | 油墨总量、叠印、描边宽度 |

### 卡 ① 画板 · 出血 · 色彩

列出出血不足 3mm 的画板及其**具体是哪条边**不足（四边全判；未贴到边的方向按 0mm 计，
空画板四边都按 0mm 报）。色彩模式 CMYK/RGB 徽标常驻，RGB 时正文红字提醒。

### 卡 ⑥ 油墨 · 叠印 · 描边粗细

按「问题类型」分块，固定顺序：

- **油墨总量 > 300**（红）——C+M+Y+K 超过 300 的对象
- **油墨总量 220~300**（黄）
- **色版 1~4%**（黄）——CMYK 某分量处于 0 与 5 之间（过浅分量单独标白）
- **叠印 N 处**（红）——处于叠印状态的对象；卡右上角并列红徽标「有叠印」
- **描边 < 0.1mm**（黄）——相同宽度合并显示，如 `0.09mm ×3`

每条样本前标 `[文字]` / `[图形]`；末尾灰字汇总正常数与 RGB / 专色 / 混合色。
长清单只列前 5 条，并在标题行右端提示「仅列出前 5 处」。

## 一键转曲（两个选项）

- **显示全部**：转曲前显示隐藏图层，并取消对象级隐藏（Ctrl+3）
- **解锁全部**：转曲前解锁图层，并解除对象级锁定（Ctrl+2）与锁定组

| 勾选组合 | 效果 |
| --- | --- |
| 两项都勾 | 解除全部隐藏与锁定，转曲文档中所有文字 |
| 只勾解锁 | 转曲其中可见的文字；仍隐藏的文字跳过 |
| 只勾显示 | 转曲其中未锁定的文字 |
| 都不勾 | 只转曲本就可见且未锁定的文字，不改动任何状态 |

## 安装

1. 关闭 Illustrator
2. 双击 `install.bat`（复制扩展并开启 CEP 未签名扩展的调试模式）
3. 重启 Illustrator → 菜单 `窗口 → 扩展功能 → 印前检查-雪糕`

卸载：双击 `uninstall.bat`，再重启 Illustrator。
更新（仅替换文件）：双击 `update.bat`，再重启 Illustrator。

## 环境要求

- Windows
- Adobe Illustrator 17.0 及以上（CC 2014+），CEP 9.0+
- 运行脚本需要 Illustrator 的 ExtendScript 环境（面板自带，无需另装）

## 已知限制

- Illustrator 脚本接口**读不到**文档「出血」的设置值，面板里的出血是**实测值**
  （对象到画板边的实际距离，容差 0.05mm）
- 一键转曲**不可撤销**，操作前建议先存盘
- 图形对象的叠印属性（`PathItem.fillOverprint` / `strokeOverprint`）**尚未在真机验证**：
  若属性名不成立，图形侧的叠印会**静默漏报**（不会崩溃、也不会误报）。文字侧走字符级
  `characterAttributes.overprintFill` / `overprintStroke`，已在 AI 28.0.0 验证可读
- 本扩展**未签名**。`install.bat` 会写入 `HKCU\Software\Adobe\CSXS.*\PlayerDebugMode`，
  这是 CEP 加载未签名扩展的**必要条件**，不是漏洞；不接受的话请不要安装

## 目录结构

```
ai-preflight-extension/
├─ index.html            面板界面
├─ css/style.css         样式
├─ js/main.js            面板前端逻辑（渲染 / 交互）
├─ jsx/preflight.jsx     宿主脚本（在 Illustrator 内运行扫描与转曲）
├─ CSXS/manifest.xml     CEP 扩展清单
├─ install.bat           安装
├─ update.bat            更新（仅替换文件）
├─ uninstall.bat         卸载
├─ README.md             本文件
└─ README.txt            完整变更日志
```

## 许可

[MIT](LICENSE)
